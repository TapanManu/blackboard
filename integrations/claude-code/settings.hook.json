{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "printf %s \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"additionalContext\\\":\\\"Board contract for this agent: name its lane (bb://<ws>/tasks.<lane>) and its output URI in the prompt; it writes a digest, never dumps raw output. Skip the board when the result is under ~1-2K tokens.\\\"}}\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
