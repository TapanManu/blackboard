# Benchmark harness — week 2, not yet built

Arms A/B/C/D and suites 1–4 are specified in [How performance will be proven](../docs/06-benchmark-plan.md); the kill
criteria they feed are in [Is this worth building — the honest case](../docs/10-is-it-worth-building.md). Nothing here runs yet.

Requires the exact tokenizer: `tokens.require_exact()` refuses to report savings
computed from the heuristic (see [measured results](../MEASURED.md)).
