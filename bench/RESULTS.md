# Write-path cost, by shape

Generated by `bench/write_shapes.py`. Re-run it; do not hand-edit.

Tokenizer: cl100k_base (exact). Payload: 12 real findings, held constant within each row.

| Write | Before | After | Saved |
|---|---:|---:|---:|
| A set of 12 findings — *body objects + echo → columns+rows* | 536 | 385 | **28%** |
| A body that already summarises itself — *digest typed again → digest_from* | 589 | 489 | **17%** |
| One small fact — *body restating digest → digest alone* | 101 | 58 | **43%** |
| Adding one finding to a running entry — *get_state(full) + re-emit → append* | 1209 | 78 | **94%** |
| The result echo, on its own — *digest echoed back → not echoed* | 81 | 26 | **68%** |

Four writes end to end (the echo row is a component of the others, not a fifth write): **2435 → 1010 tokens, 59% less**.

Reading these: each row is a *whole write* — uri, digest, arguments and the result that comes back. The 34% the skill quotes for `columns`+`rows` is the row payload alone, which is why the same change reads lower here: the digest and the reply are identical on both sides and dilute the ratio. Neither number is wrong; they measure different brackets.
